apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
      # Change the value of image field below to your controller image URL. This will need updating 
      # with the registry
      - image: docker.io/weaveworks/cluster-api-microvm-controller:v0.1.0
        name: manager
