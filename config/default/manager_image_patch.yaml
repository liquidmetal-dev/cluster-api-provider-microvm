apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
      # Change the value of image field below to your controller image URL. This will need updating 
      # with the registry
      - image: cluster-api-microvm-controller:dev
        name: manager